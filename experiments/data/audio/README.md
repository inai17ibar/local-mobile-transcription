# Audio Dataset

評価用音声を置きます。音声データは機密になりやすいため、このディレクトリの中身は `.gitignore` で除外しています。

- 推奨: `20-50` 本
- 混ぜる条件: `short`, `noise`, `multi_speaker`, `proper_noun`
- `sample.m4a` の分割結果は `sample_seed/` などのサブディレクトリに配置
